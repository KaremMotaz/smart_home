import 'package:flutter/material.dart';

class AllRoomsView extends StatelessWidget {
  const AllRoomsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("All Rooms View")));
  }
}
