import 'package:flutter/material.dart';

class SoundView extends StatelessWidget {
  const SoundView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Sound View")));
  }
}
