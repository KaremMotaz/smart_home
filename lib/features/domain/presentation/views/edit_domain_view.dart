import 'package:flutter/material.dart';

class EditDomainView extends StatelessWidget {
  const EditDomainView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Edit Domain"),
      ),
    );
  }
}
