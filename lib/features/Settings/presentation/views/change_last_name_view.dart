import 'package:flutter/material.dart';

class ChangeLastNameView extends StatelessWidget {
  const ChangeLastNameView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Column(children: [Text("ChangeLastNameView")])),
    );
  }
}
